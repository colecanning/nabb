# Enforce pnpm usage
# This prevents accidentally using npm or yarn
engine-strict=true

# Use the version specified in package.json
package-manager-strict=true

# Shamefully hoist to avoid potential issues with Next.js and React
shamefully-hoist=true

# Auto install peers
auto-install-peers=true
